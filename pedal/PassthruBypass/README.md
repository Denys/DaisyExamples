# PassthruBypass

Minimal DaisyPedal hardware example.

- `SW_BYPASS` toggles the relay bypass path with a short mute window
- `SW_AUX` taps tempo for the heartbeat LED
- `LED_1` shows DSP active vs relay bypass
- `LED_2` pulses at the tapped tempo
- OLED displays current mode and tempo
