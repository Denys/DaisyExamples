# NoiseGate

Simple stereo gate based on the new `DaisySP::NoiseGate` helper.

- `SW_BYPASS` toggles the processed path
- `KNOB_1` threshold
- `KNOB_2` hysteresis
- `KNOB_3` attack
- `KNOB_4` release
- `KNOB_5` closed gain range
- `KNOB_6` output level
