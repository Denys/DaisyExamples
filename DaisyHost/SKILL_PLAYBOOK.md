# DaisyHost Skill Playbook

Last updated: 2026-04-28

Use this file for DaisyHost skill-related activities:

- skill selection for a task or task class
- `Expected UF` vs `Observed UF`
- evidence and validation notes
- skill activity logs
- refinements or corrections to reusable skill workflows

`PROJECT_TRACKER.md` records that a skill was used in an iteration.
`SKILL_PLAYBOOK.md` records how well the skill actually performed.

Skill notes should follow the DaisyHost manager-language rule: summarize what
the skill changed for planning or implementation, why it mattered, what it
unlocked, what friction or blockers remained, and what evidence supports reuse
before diving into detailed file/test evidence.

## UF Definitions

- `Expected UF`:
  - expected usefulness for DaisyHost tasks of the relevant class
  - a prior estimate based on task fit, prerequisites, and expected quality gain
  - not validated by the skill description alone
- `Observed UF`:
  - evidence-based usefulness from actual DaisyHost task use
  - update only after a materially used skill produced an observable outcome
  - keep as `Pending` when there is no meaningful DaisyHost evidence yet

## Validation Method

Evaluate `Observed UF` per task class, not globally.

For each materially used skill, capture:

- task class
- date
- expected UF before use
- actual outcome
- concrete evidence
- blockers or friction
- whether the skill should be reused for the same DaisyHost task class

Recommended scoring dimensions, each on `0..5`:

| Dimension | Meaning |
|---|---|
| Task fit | How directly the skill matched the DaisyHost task |
| Quality gain | How much it improved the output or reduced drift/regressions |
| Time gain | How much it reduced rework or iteration count |
| Reliability | How usable it was without auth/tooling/process blockers |
| Reuse value | How likely it is to help again on similar DaisyHost work |

Recommended weighted formula:

```text
Observed UF =
0.30 * Quality gain +
0.25 * Task fit +
0.20 * Time gain +
0.15 * Reliability +
0.10 * Reuse value
```

Round to the nearest tenth or nearest integer. If the evidence is too weak,
leave `Observed UF` as `Pending`.

## Current Skill Register

| Skill | Primary DaisyHost use | Expected UF | Observed UF | Sample size | Evidence status | Caveat | Improvement note | Last validated |
|---|---|---|---|---|---|---|---|---|
| `superpowers:test-driven-development` | Code iterations touching production behavior, tests, or bug fixes. | `5/5` | `5/5` | `21` | Observed in the 2026-04-22 W4/W5 package through the 2026-04-28 TF15 doctor readiness pass: helper-layer, hosted-app, firmware, Python-tooling, route-contract, CLI payload, tracker-tool, board-editor metadata, gate-diagnostics, and doctor-diagnostics red/green flow forced hosted-app seams, board seams, render contracts, snapshot evidence, smoke coverage, selected-node Field proof, route-contract wording, external `debugState` payload shape, next-WP parsing/scoring behavior, profile-backed editor-surface policy, gate JSON parsing/classification, and doctor readiness checks to move before closeout. | C++ helper work still needed interface/header scaffolding before the first red build could link, dirty worktree state can make red evidence depend on current checkout content rather than net diff alone, parallel slices still need an integrator pass to close final contract gaps, JUCE thread-race regressions need a future host-side stress harness before they can be truly red-first, and firmware red steps can only prove build wiring until hardware is flashed and manually exercised. | Add DaisyHost-native red-step templates for helper-layer, hosted-app imports, processor-adjacent JUCE work, board-profile metadata, board-editor surface policy, audio/message-thread stress coverage, Field firmware adapter scaffolds, Python CLI/golden-output tooling, route-contract hardening checks, CLI JSON payload additions, gate diagnostics, doctor readiness checks, and tracker-derived workflow utilities. | `2026-04-28` |
| `verification-before-completion` | Fresh build/test gate verification before any DaisyHost success claim or closeout. | `5/5` | `5/5` | `25` | Observed in the 2026-04-22 W4/W5 package through the 2026-04-28 TF15 doctor readiness pass: it held the line between what was truly green, what was flash-verified, what was only build/QAE-verified, what still needs hands-on hardware validation, which Field/DAW claims remain deferred, which PolyOsc support is host-side only, which CLI counts are current versus historical, when TF10 could move from partial to complete, when WS10 could claim only additive CLI payload readback, when a tracker-tooling change required a new CTest plus full gate, when TF9 could claim completion only after targeted tests, when TF14 needed both direct `gate --json` proof and the underlying wrapper rerun, and when TF15 needed direct `doctor --json`, focused CTest, and full gate evidence. | Full verification is slower and still depends on surfacing exact commands/results in the tracker. Manual GUI and hardware validation must be described exactly when the behavior is outside the automated harness. | Keep the wrapper command plus any required firmware/manual validation commands copied verbatim into tracker/checkpoint closeouts, and explicitly log when an external lock, DAW validation gap, hardware checklist gap, generated-adapter flash gap, host-only support boundary, stale-count distinction, harness gap, product-scope boundary, additive-CLI-only boundary, workflow-tooling-only boundary, board-profile-only boundary, diagnostic-wrapper boundary, or doctor-preflight boundary remains. | `2026-04-28` |
| `systematic-debugging` | Unexpected DaisyHost build/test/runtime failures where the first obvious fix may be wrong. | `4/5` | `5/5` | `6` | Observed in the 2026-04-22 verification-hardening pass, the 2026-04-23 Braids hosted-app pilot, the earlier 2026-04-24 verification hardening pass, the later same-day rack-freeze resume, the later same-day rack-freeze mitigation attempt, and the 2026-04-25 knob-drag crash fix: it prevented blaming one app or the Field panel, isolated the common knob/control path, and identified concurrent message-thread/audio-thread access to the hosted app core as the shared cause. | The workflow is slower up front because it insists on reproducing failures outside the main build command before fixing. GUI/audio-thread races still need manual stress validation unless a dedicated harness exists. | Add a DaisyHost-specific JUCE/MSBuild triage checklist for helper-path, host-toolchain-compatibility, post-link executable locking, machine-level security blockers, and hosted-core audio/message-thread races. | `2026-04-25` |
| `coderabbit:code-review` | Coherent diff review or milestone handoff after local testing has passed. | `4/5` | `Pending` | `0` | No DaisyHost-local observed sample set yet in this file. | Requires a real git worktree plus CodeRabbit auth; this checkout does not currently satisfy that prerequisite. | Define a DaisyHost review-scope policy for `uncommitted` vs `committed` review and note the standard config-file set to pass with `-c`. | `Pending` |
| `notebooklm` | DaisyBrain architectural recall when local docs are insufficient. | `3/5` | `Pending` | `0` | No DaisyHost-local observed sample set yet in this file. | Strategic memory only; must not override local source-of-truth docs. | Add a small DaisyHost prompt cookbook and keep the "local docs first, NotebookLM second" rule explicit. | `Pending` |
| `writing-plans` | Source-backed implementation plans for multi-step DaisyHost features before code starts. | `4/5` | `4/5` | `3` | Observed in the 2026-04-24 Daisy Field implementability pass, follow-up refinement, and 2026-04-25 Field project tracker creation: it first separated the Field plan into testable milestones, then tightened the package boundary, and later organized remaining Field options into sprint-sized implementation packages. | The generic template asks for very granular implementation steps and assumes a clean execution handoff; DaisyHost planning still needs repo-specific gate language, dirty-worktree caution, and explicit "do not touch" boundaries. | Keep using it for feature plans, but prepend a DaisyHost-specific implementability summary, gate status, and package boundary before task breakdown. | `2026-04-25` |
| `executing-plans` | Executing already-approved DaisyHost implementation plans while preserving explicit out-of-scope boundaries. | `4/5` | `4/5` | `13` | Observed in the 2026-04-24 Field board-support shell through the 2026-04-28 TF15 doctor readiness pass: it kept each package within its requested scope while preserving `daisy_patch` default behavior, the frozen rack, flash-vs-manual hardware boundaries, semi-automatic generation limits, host-only Field-surface boundaries, the TF12 no-new-commands boundary, the TF10 no-new-route-semantics boundary, the WS10 additive-existing-CLI boundary, the TF9 no-new-board/no-new-runtime-behavior boundary, the TF14 build-semantics/no-doctor-expansion boundary, and the TF15 existing-command/no-gate-execution boundary. | The generic workflow assumes cleaner branch isolation than this shared dirty checkout often has; a single integrator still has to reconcile worker output and docs, and firmware/tooling plans need explicit hardware-validation stop points. | Reuse for approved implementation plans, but keep DaisyHost's local docs, firmware build evidence, generated-tool evidence, host-only boundaries, current repo state, dirty-worktree ownership, product/foundation split, additive-vs-new-command distinction, board-profile-vs-product-behavior distinction, diagnostic-vs-build-semantics boundary, and doctor-preflight-vs-gate-execution boundary ahead of stale plan text. | `2026-04-28` |
| `daisy-cpp` | Board-specific Daisy hardware verification for host plans that depend on real Field/Pod/Seed control surfaces. | `4/5` | `4/5` | `6` | Observed in the 2026-04-24 Daisy Field implementability pass, the Field board-support shell implementation pass, the 2026-04-25 Field native controls pass, the 2026-04-25 Field extended host surface pass, the 2026-04-25 Field firmware adapter pass, and the 2026-04-25 HW/App adapter pipeline pass: it anchored the work in local `DaisyField` facts, kept host-side Field simulation separate from firmware, guided the first `field/MultiDelay` adapter through build/QAE/ST-Link flash, and kept generated adapter output aligned with main-loop control processing plus a real-time-safe audio callback. | The skill is firmware-oriented, so host-side JUCE work still needs DaisyHost-local source inspection to decide what is live-host behavior versus firmware parity; it also cannot replace hands-on audio/CV validation or justify arbitrary firmware source translation. | Reuse for multi-board DaisyHost planning, Field firmware work, and generated adapter tooling, then verify implementation surfaces against local DaisyHost contracts and record manual hardware results separately. | `2026-04-25` |
| `doc-coauthoring` | Multi-doc DaisyHost maintenance where tracker/checkpoint/readme/agents/changelog need to stay aligned. | `3/5` | `3/5` | `9` | Observed in the 2026-04-22 skill-playbook extraction/docs-sync pass, the same-day verification-hardening closeout, the 2026-04-23 WS6/WS7 closeout, the later same-day visible-rack closeout, the later same-day Braids closeout, the 2026-04-24 verification hardening pass, the later same-day rack-freeze mitigation closeout, the 2026-04-25 Field project tracker creation, and the 2026-04-25 PolyOsc hosted-app closeout; helped keep the new hosted-app status, evolving unit-test-payload path, verification wording, Field-specific follow-on tracker, and PolyOsc host-only support boundary aligned across the tracking docs. | The default workflow is heavier than most DaisyHost maintenance passes need. | Define a lightweight repo-maintenance variant focused on synchronized tracking-doc updates. | `2026-04-25` |
| `subagent-driven-development` | Parallel DaisyHost packages that can be cleanly split into app-core, docs, and integrator slices. | `4/5` | `4/5` | `5` | Observed in the 2026-04-23 WS6/WS7 package, the later same-day visible-rack WS7 sprint, the later 2026-04-24 Harmoniqs/VA Synth app wave, the 2026-04-25 Field refinement closeout, and the 2026-04-25 Field ergonomics/UI cleanup: bounded side lanes helped with audit/review and board-profile red coverage while the main thread kept ownership of implementation, integration, and verification. | Background agents can leave partial work or incomplete narrative handoff if they are not closed promptly once integration starts; writable lanes need tight ownership boundaries in a dirty checkout. | Keep ownership slices narrow and force an explicit integrator checkpoint before the full verification run. | `2026-04-25` |
| `dispatching-parallel-agents` | Choosing when to split DaisyHost work into bounded side lanes versus keeping the critical path local. | `4/5` | `4/5` | `1` | Observed in the 2026-04-25 Field ergonomics/UI cleanup: one Lane B worker added board-profile red tests for generic Field surface lookup while the main thread implemented the mapping lane and later integrated the editor/profile fix. | The lane was useful but crossed near the editor/profile boundary, so the main thread still needed to inspect and complete the production edit. | Use for independent test/audit lanes, then close with a single integrator pass before verification. | `2026-04-25` |

## Planned Evaluation Queue

Predeclare likely skill use here before code starts when an iteration is
expected to produce fresh DaisyHost evidence.

| Date | Planned skill | Task class | Expected UF | Why it is being queued now | Evidence target |
|---|---|---|---|---|---|
| 2026-04-23 | `superpowers:test-driven-development` | Visible rack topology, v5 session state, and node-targeted render/runtime contract | `5/5` | The visible-rack sprint changes routing invariants and persistence shapes, so helper and contract tests should define legal topology before live runtime glue expands. | Red helper/contract checks fail for unsupported topology or missing rack fields, then pass after implementation and reruns. |
| 2026-04-23 | `subagent-driven-development` | Visible 2-node live rack sprint across topology/helper, editor/UI, and integrator slices | `4/5` | The sprint has three cleanly parallelizable lanes: topology helper/tests, rack editor/board seam, and processor/session/render integration. | Worker slices land bounded code without cross-slice churn, then the integrator closes the runtime and docs with one verification pass. |
| 2026-04-23 | `verification-before-completion` | Visible-rack sprint closeout | `5/5` | The sprint touches processor, editor, session, and render surfaces, so closeout must separate fully green proof from any remaining environment blocker. | Build/smoke/render proofs plus any blocked full-gate step are logged explicitly before the tracker/checkpoint/readme/changelog are updated. |
| 2026-04-23 | `doc-coauthoring` | Cross-doc visible-rack closeout | `3/5` | The sprint changes manager-facing product status and the Daisy Field staffing rule, so multiple local docs need synchronized wording. | Tracker/checkpoint/readme/changelog all reflect the same rack status, unlock language, and remaining blocker. |
| 2026-04-23 | `superpowers:test-driven-development` | WS6 `MetaControllers` semantic-mapping and arbitration contract | `5/5` | `MetaControllers` introduce new behavior at the canonical-parameter boundary and should be pinned down with red/green contract tests before processor or UI glue expands. | Red tests fail for macro-to-parameter mapping, arbitration, or state-restoration expectations, then pass after implementation and a targeted rerun. |
| 2026-04-23 | `superpowers:test-driven-development` | WS7 rack-ready session/render contract | `5/5` | Rack-ready session/render work changes persistence and render invariants, so the contract should be specified in tests before node-scaling integration starts. | Red tests fail for node-scoped session or render-contract expectations, then pass after implementation and targeted host/render reruns. |
| 2026-04-23 | `verification-before-completion` | Combined WS6/WS7 package closeout | `5/5` | This package spans state, render, and likely host-surface changes, so no landed claim should be written without targeted evidence plus the full host gate. | Targeted WS6/WS7 tests and the full DaisyHost host gate are rerun before tracker/checkpoint/readme/changelog closeout claims. |
| 2026-04-23 | `doc-coauthoring` | Cross-doc closeout after WS6/WS7 verification | `3/5` | If the combined package lands, multiple local docs will need synchronized status and verification wording without overstating in-progress behavior. | Post-verification doc pass keeps tracker/checkpoint/readme/changelog language aligned and clearly dated. |
| 2026-04-22 | `superpowers:test-driven-development` | Host automation bridge helper and slot-mapping tests | `5/5` | W4 is a new behavior surface with a clear red/green helper-test path. | Red helper tests fail for the expected bridge contract, then pass after implementation and host-gate rerun. |
| 2026-04-22 | `superpowers:test-driven-development` | Effective-state snapshot/readback helper and contract tests | `5/5` | W5 is also a new behavior surface and should be specified through helper-layer tests before processor glue lands. | Red helper tests fail for snapshot contract gaps, then pass after implementation and host-gate rerun. |
| 2026-04-22 | `doc-coauthoring` | Cross-doc closeout for tracker, checkpoint, readme, changelog, and skill playbook | `3/5` | This package changes both runtime capability and local workflow truth, so cross-doc sync should be evaluated explicitly. | Post-verification doc pass keeps workstream status, unlock language, and test evidence aligned without stale claims. |

## Skill Activity Log

Log only materially used skills here. Referencing a skill as a source of
phrasing or policy without actually exercising its workflow does not count as a
material use.

| Date | Skill | Task class | Expected UF | Outcome | Observed UF contribution | Evidence | Blockers / friction | Reuse verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-04-28 | `executing-plans` + `superpowers:test-driven-development` + `verification-before-completion` + `subagent-driven-development` | TF15 doctor source/build readiness expansion | `4/5`, `5/5`, `5/5`, `4/5` | Executed the approved TF15 plan as a readiness expansion of existing `doctor --json`: red tests pinned the missing helper contract, production code added deterministic source/build/ctest/environment/blocker JSON, a docs sidecar updated the manager-facing wording, and verification kept doctor as preflight rather than a gate runner. | `5/5` for TDD/verification; `4/5` for execution/subagents because the shared dirty checkout required final integrator reconciliation and normalized-env proof. | Red Debug build failed on missing `DoctorDiagnostics.h`; Debug `DoctorDiagnostics|DaisyHostCliDoctor` CTest passed `9/9`; normalized-env Release `DaisyHostCLI unit_tests` built; direct Release `doctor --json` returned `ok: true` with no blockers; Release focused CTest passed `9/9`; final `cmd /c build_host.cmd` passed Release `ctest` `269/269`; next-WP recommender selected `TF12`, runner-up `WS10`. | Existing dirty work remains; raw unsanitized doctor can correctly report `duplicate-path-env`; a parallel Debug/Release build collision was rerun cleanly. TF15 did not add a new command, execute the gate inside doctor, automate GUI/live plugin/DAW behavior, flash firmware, validate hardware, alter routing, or provide generic shell control. | Reuse this stack for future CLI diagnostics where helper-level JSON can be tested first, executable proof follows, and final docs wait for actual gate evidence. |
| 2026-04-28 | `executing-plans` + `superpowers:test-driven-development` + `verification-before-completion` | TF14 CLI gate diagnostics | `4/5`, `5/5`, `5/5` | Executed the approved TF14 plan as a thin diagnostics wrapper over `build_host.cmd`: red tests pinned the missing helper contract, production code added deterministic phase/CTest/blocker JSON, and verification kept the no-build-semantics/no-doctor-expansion boundary intact. | `5/5` for TDD/verification; `4/5` for execution because a transient locked-artifact and payload-copy failure required reruns before final gate proof. | Red Debug build failed on missing `GateDiagnostics.h`; green `GateDiagnostics|CliPayloads` CTest passed `17/17`; Release `DaisyHostCLI unit_tests` built; direct `gate --json` passed with CTest `244/244`; final `cmd /c build_host.cmd` passed Release `ctest` `244/244`. | Existing dirty work remains; TF14 did not add `doctor` readiness checks, GUI automation, live plugin control, DAW/VST3 validation, firmware flashing, or generic shell/git wrapping. | Reuse this stack for future CLI diagnostics slices where helper-level JSON behavior can be tested before executable proof. |
| 2026-04-28 | `executing-plans` + `superpowers:test-driven-development` + `verification-before-completion` | TF9 board-generic editor surface completion | `4/5`, `5/5`, `5/5` | Completed the approved TF9 plan as a board-profile contract pass: red tests pinned missing editor-surface metadata, production code moved board-facing hint/keyboard/trace/indicator/extended-surface policy into `BoardProfile`, and the editor now consumes that metadata without adding new board/runtime behavior. | `5/5` for TDD/verification; `4/5` for execution because the dirty checkout and transient artifact/payload locks required reruns. | Red Debug build failed on missing `editorSurface` / `BoardEditorTraceMode`; green targeted Debug CTest passed `41/41` and `59/59`; Debug standalone smoke passed for Patch/Torus and Field/Torus; CLI `describe-board` Patch/Field passed; final `cmd /c build_host.cmd` passed Release `ctest` `243/243` after transient VST3 lock and unit-payload permission reruns. | Existing dirty work remains; no new board, routing preset, graph editor, firmware, Field hardware, mixed-board behavior, manual GUI screenshot review, or DAW/VST3 validation. | Reuse this stack for board-profile/editor-surface foundation work; keep future product workflow in `WS11`; TF15 doctor readiness has since landed as separate CLI preflight scope. |
| 2026-04-27 | `brainstorming` + `superpowers:test-driven-development` + `verification-before-completion` | Repo-wide next-WP recommendation workflow | `2/5`, `5/5`, `5/5` | Implemented a checked tracker-derived recommendation tool rather than another prose-only rule: red tests pinned missing parser/recommender behavior, the tool now emits recommendation/runner-up/wait decisions, and closeout reran the registered CTest plus full host gate. | `5/5` for TDD/verification; `2/5` for brainstorming because the skill's heavy clarification flow was too much for a directly approved small workflow implementation, though the brief design pass helped keep the scope to tracker truth. | Red `py -3 tests\test_next_wp_suggester.py` failed on missing `tools/suggest_next_wp.py`; green direct tests passed `2 tests`; `DaisyHostNextWpSuggester` CTest passed `1/1`; full `cmd /c build_host.cmd` passed Release `ctest` `233/233`; tool currently recommends `TF9`, runner-up `TF14`, waits `WS9`, `WS11`, `WS12`, `WS13`, `TF17`, `TF18`. | Existing dirty work remains across host/editor/docs/submodules; this was workflow/tooling only and did not validate runtime behavior, hardware, firmware, mixed-board racks, or DAW/VST3. | Reuse TDD and verification for repo workflow utilities; use brainstorming lightly only when the workflow shape is unclear, not as a blocking ceremony for small approved tasks. |
| 2026-04-27 | `executing-plans` + `superpowers:test-driven-development` + `verification-before-completion` | WS10 external debug surface first slice | `4/5`, `5/5`, `5/5` | Executed the approved WS10 plan as an additive payload change on existing CLI commands: red tests pinned missing `debugState`, implementation reused snapshot/render readback, and verification kept the no-new-command/no-routing-semantics boundary intact. | `5/5` for TDD/verification; `4/5` for execution because the shared dirty checkout and known raw MSBuild Path/PATH issue required a normalized-env CLI build rerun. | Red `CliPayloadsTest` failed `2/8`; green `CliPayloadsTest` passed `8/8`; direct Debug CLI snapshot/render checks returned `debugState`; broader Debug subset passed `76/76`; full `cmd /c build_host.cmd` passed Release `ctest` `232/232`. | Existing dirty work remains across host/editor/docs/submodules; no new CLI command, route semantics, topology preset, firmware, Field hardware, mixed-board behavior, or DAW/VST3 validation was run. | Reuse this stack for additive CLI/readback slices where payload shape can be pinned with helper tests before executable smoke. |
| 2026-04-27 | `executing-plans` + `superpowers:test-driven-development` + `verification-before-completion` | TF10 routing contract hardening | `4/5`, `5/5`, `5/5` | Executed the approved hardening plan without expanding WS9 product scope: red tests caught sprint-era unsupported-route wording, then the production change made the current two-node audio contract durable and documented. | `5/5` for TDD and verification; `4/5` for execution because the shared dirty checkout required a surgical local pass instead of a clean worktree. | Red `LiveRackTopology` CTest failed `1/17`; green topology CTest passed `17/17`; affected render/session/snapshot Debug CTest passed `51/51`; full `cmd /c build_host.cmd` passed Release `ctest` `230/230`. | Existing dirty work remains across host/editor/docs/submodules; no DAW/VST3, firmware, hardware, graph-editor, larger-rack, non-audio-route, or new-preset validation was run. | Reuse for final foundation hardening passes that need manager-readable closeout while keeping product semantics in a separate workstream. |
| 2026-04-27 | `superpowers:test-driven-development` + `systematic-debugging` + `verification-before-completion` | TF8/TF9 Field UI follow-up: A/B legend, external I/O alignment, Field LEDs, cutoff safety, and Mod lane detail text | `5/5`, `4/5`, `5/5` | Kept the UI/control follow-up contract-first: red tests pinned missing legend layout, non-overlapping hardware-style external control rows, Subharmoniq Field key LED state, Field external cutoff safety, and Mod lane display text before production code landed. Systematic debugging identified the cutoff symptom as Field external control/modulation driving cutoff-like targets to normalized `0.0`. Verification kept raw MSBuild environment failure separate from code status. | `5/5` for TDD and verification; `4/5` for systematic debugging because the audio root cause is host-harness proven but not manually audio-listened. | Red `cmake --build build --config Debug --target unit_tests` failed on the missing contracts; green targeted Debug CTest passed `89/89`; normalized-env Debug `DaisyHostPatch_Standalone` build passed; Debug standalone smoke passed for `daisy_patch/torus` and `daisy_field/torus`; `git diff --check` passed with LF/CRLF warnings. | Raw standalone build hit the known duplicate `Path` / `PATH` MSBuild issue before editor compilation; manual visible GUI inspection, hardware LED/CV validation, real audio listening, and DAW/VST3 checks were not run. | Reuse this stack for dense Field UI/control fixes, but add screenshot/manual review when the requested acceptance criteria are primarily visual. |
| 2026-04-27 | `executing-plans` + `superpowers:test-driven-development` | TF11 node-targeted event/readback contract slice | `4/5`, `5/5` | Executed the approved TF11 prompt as a bounded contract-first slice parallel to TF10: tests first pinned missing resolved node-target readback and missing CLI render debug arrays, then production code filled only render/readback payloads without changing routing semantics. | `5/5` for TDD shape and final verification; `4/5` for execution because stale standalone smoke processes had to be cleared before the final wrapper pass. | Red targeted CTest failed on missing `targetNodeId` values and missing CLI render `nodes`; green targeted TF11 CTest passed `2/2`; broader Debug contract subset passed `71/71`; full Debug CTest passed `227/227`; full `cmd /c build_host.cmd` passed Release `ctest` `227/227`; a direct render manifest proof showed resolved `node0`, `node1`, `node1`, `node1` targets. | Two wrapper attempts failed only because stale standalone smoke processes were already running; no manual Field hardware, firmware flashing, or DAW/VST3 validation was run. | Reuse for node/event/readback contract work; before final wrapper gates, check for stale standalone/hub processes that smoke tests will reject. |
| 2026-04-27 | `executing-plans` + `superpowers:test-driven-development` + `systematic-debugging` | TF10 routing contract generalization | `4/5`, `5/5`, `4/5` | Executed the approved TF10 contract-first plan as a partial route-plan slice: red tests pinned the missing shared route-plan API, implementation centralized topology route planning, and render/live processing now consume the shared plan without adding routing presets. | `5/5` for TDD on the shared contract; `4/5` for execution because initial full-gate closeout was blocked outside TF10, then later recovered during TF11 closeout. | Red Debug build failed on missing `LiveRackRoutePlan` / `TryBuildLiveRackRoutePlan`; targeted `LiveRackTopology` passed `14/14`; targeted render chain checks passed `5/5`; full `RenderRuntime` CTest passed `31/31`; `HostSessionState|EffectiveHostStateSnapshot` passed `18/18`; Debug wrapper build with `-SkipTests` passed; later full `cmd /c build_host.cmd` passed Release `ctest` `227/227`. | Initial full-gate attempt was blocked by unrelated dirty test/API drift; direct raw MSBuild still needs the wrapper or sanitized environment. | Reuse this stack for contract-first foundation slices, and keep final routing-preset semantics owned by explicit `WS9` scope. |
| 2026-04-27 | `executing-plans` + `superpowers:test-driven-development` + `dispatching-parallel-agents` + `verification-before-completion` | WS8 rack UX productionization | `4/5`, `5/5`, `4/5`, `5/5` | Executed the approved WS8 plan as a polish pass over the existing rack, not a routing rewrite: red tests pinned missing operator-facing topology/role helpers, a read-only sidecar reviewed doc update targets, and the main thread integrated rack labels, selected-node context, and Patch/Field target hints. | `5/5` for TDD/verification evidence; `4/5` for execution/delegation because the useful parallel work was bounded to doc review while integration stayed local. | Red normalized-env Debug build failed on missing rack display helpers; green Debug `unit_tests` built; targeted Debug CTest passed `96/96`; Debug standalone built; focused Field/RSP status CTest passed `69/69`; Release render smoke passed; full `cmd /c build_host.cmd` passed with Release `ctest` `216/216`. | A first full-gate attempt hit `LNK1104` because `DaisyHost Hub.exe` was running, and one wrapper retry was aborted before output. Manual visible GUI inspection, Field hardware validation, DAW/VST3 validation, generated-adapter flashing, real CV voltage measurement, and DaisyHostController USB MIDI validation were not run. | Reuse this stack for bounded operator-facing rack/UI polish where helper labels can be tested, but keep screenshot/manual validation separate from automated host gates. |
| 2026-04-27 | `brainstorming` + `superpowers:test-driven-development` + `frontend-design` + `verification-before-completion` | DaisyHost Field UI / RSP readability refinement | `4/5`, `5/5`, `3/5`, `5/5` | Kept the requested visual refinement bounded to Field panel/readability work: red tests pinned the missing key legend, external-control row grouping, and roomier CV-generator card geometry before the profile/editor changes landed. | `5/5` for TDD/verification evidence; `3/5` for frontend-design guidance because the task was JUCE/native UI rather than a web frontend. | Red CTest failed on the intended missing contracts; `.\build_host.cmd -Configuration Debug -SkipTests` passed; targeted Debug CTest passed `38/38`; Debug standalone smoke passed for Patch/Torus and Field/Torus. | The direct raw MSBuild build hit the known duplicate `Path` / `PATH` environment issue, so wrapper build proof was required. Manual visible-GUI inspection was not run. | Reuse TDD and verification for future Field UI/layout changes; use frontend-design only as lightweight visual guidance for native JUCE surfaces. |
| 2026-04-26 | `executing-plans` + `superpowers:test-driven-development` + `systematic-debugging` + `verification-before-completion` | DaisyHost host modulation-lane architecture | `4/5`, `5/5`, `4/5`, `5/5` | Executed the approved modulation-lane plan as a source-backed vertical slice: helper evaluation, v6 session persistence, snapshot/CLI readback, processor effective-parameter application, cross-app effective override support, and compact Field `Mod` drawer UI. Systematic debugging isolated the only new test crash to a test iterator invalidation bug, not the implementation. | `5/5` | Red build first failed on missing `HostModulation.h`; later Debug `unit_tests` built; targeted Debug CTest passed `61/61`; Debug `DaisyHostPatch_Standalone` built; final `cmd /c build_host.cmd` passed with Release `ctest` `211/211` after stopping a stale standalone process that locked the Release executable. | MSBuild required the known local `Path` / `PATH` sanitization and single-threaded `/m:1` for direct Debug commands to avoid environment/PDB issues. GUI behavior is build-backed but not manually clicked in a visible standalone session. | Reuse this skill stack for future processor/editor architecture changes that need helper-level red/green tests plus final build evidence. |
| 2026-04-26 | `systematic-debugging` + `superpowers:test-driven-development` + `verification-before-completion` | DaisyHost Field key UI disabled-state regression | `5/5` | Root-cause tracing found the editor still used MIDI-note presence as the clickability criterion after Subharmoniq Field keys moved to menu-action bindings. TDD pinned the correct binding-level interactivity rule before changing the editor. | `5/5` | Red Debug build failed on missing `IsDaisyFieldKeyInteractive`; green Debug `unit_tests` built, targeted CTest passed `34/34`, and Debug `DaisyHostPatch_Standalone` built with existing editor C4702 warnings. | The GUI was not manually clicked in a visible standalone session; verification is source/build/test-backed. | Reuse for UI disabled-state bugs where binding availability and visual interactivity can drift apart. |
| 2026-04-26 | `executing-plans` + `verification-before-completion` | TF12 verification/build hardening adoption slice | `4/5` and `5/5` | Kept the approved TF12 slice scoped to docs plus verification adoption: no new CLI commands were added, stale verification counts were demoted to history, and current truth moved to a fresh full gate. | `4/5` and `5/5` | `cmd /c build_host.cmd` passed with Release `ctest` `202/202`; direct Release CLI checks passed for `doctor`, `list-apps`, `describe-app cloudseed`, `describe-board daisy_field`, `validate-scenario`, `render` with checksum `c9c3f665e6a0dd2b`, and `smoke --mode render`; scoped `git diff --check` passed. | The shared checkout remains broadly dirty, and manual DAW/VST3, computer-side USB MIDI, and hands-on Field hardware validation were not run. | Reuse for verification/doc-hardening passes where old evidence must be reconciled without expanding product scope. |
| 2026-04-26 | `systematic-debugging` + `superpowers:test-driven-development` + `verification-before-completion` | DaisyHost Subharmoniq Field key/audio regression coverage | `5/5` | Kept the reported `A7`/`B7` issue split into semantics and host-path evidence: `A7` is a Rhythm 3 assignment key, while `B7` is transport, and both direct core and Field surface render tests now prove the key sequence produces audio. | `5/5` | Debug `unit_tests` built; targeted CTest passed `11/11`, including `SubharmoniqCoreTest.FieldB7StartsAudioAndBothSequencersAfterA7RhythmEdit` and `RenderRuntimeTest.SubharmoniqFieldB7StartsAudioAfterA7RhythmEdit`. | The automated harness cannot physically press the Field buttons or capture analog audio from hardware. | Reuse this pattern when a reported host control bug may be either a control-label misunderstanding or a missing surface-event route. |
| 2026-04-26 | `systematic-debugging` + `superpowers:test-driven-development` + `daisy-cpp` | SubharmoniqField A/B key-row correction plus DaisyHost Subharmoniq Field-key action mapping | `5/5` | Avoided changing only raw button numbers by first identifying the row-level inversion from hardware behavior, then added a host contract so `subharmoniq` Field keys are app actions while other apps keep MIDI-key behavior. | `5/5` | Red Debug build failed on missing Field-key binding contract; green Debug targeted CTest passed `11/11`; firmware `make` passed with FLASH `126072 B` / `96.19%`; QAE passed `0/0`; `make program` verified OK; 2s OpenOCD check halted in Thread mode, not HardFault. | Full wrapper host gate and hands-on B7/B8/audio confirmation were not run in this pass. Flash usage is now tight at `96.19%`. | Reuse for hardware-label/control-surface bugs where firmware scan indexes and host surface semantics must stay aligned. |
| 2026-04-26 | `superpowers:test-driven-development` + `executing-plans` + `verification-before-completion` | Native DaisyHostCLI for agent/CI automation | `5/5` | Kept the CLI as a thin wrapper around existing app/board/render/snapshot contracts and forced a real red step before implementation. | `5/5` | Red build failed on missing `daisyhost/CliPayloads.h`; targeted Debug `CliPayloadsTest` passed `4/4`; Debug CLI command checks and `DaisyHostCli` CTest passed `5/5`; full `cmd /c build_host.cmd` passed Release `ctest` `197/197`; Release CLI `smoke --mode render` and `doctor` passed. | Raw MSBuild still needs the wrapper because the default shell can expose duplicate `Path` / `PATH`; no firmware path was touched. | Reuse this pattern for future agent-facing host utilities: helper-layer JSON tests first, then executable smoke, then full wrapper gate. |
| 2026-04-26 | `brainstorming` + `superpowers:test-driven-development` + `verification-before-completion` | DaisyHost Field Page 1 modulation-bay redesign and keyboard viewport | `5/5` | Produced buildable code, but the visual interpretation was wrong: the expanded modulation bay made DaisyHost/RSP larger, heavier, and still did not solve stacking. The change was reverted to the compact split Page 1 layout. | `2/5` | Original red/green path built and passed tests, but user visual feedback rejected the result. Rollback proof: helper symbols removed, targeted Debug CTest passed `27/27`, Debug standalone built, and full `cmd /c build_host.cmd` passed Release `ctest` `196/196`. | Unit/build tests did not catch the layout failure. Screenshot-heavy UI work needs a visible standalone screenshot review before treating it as good. | Do not reuse this as a success pattern. For dense Field drawer redesign, keep geometry small, preserve perceived lightness, and verify with actual screenshot inspection before docs/gate closeout. |
| 2026-04-26 | `daisy-cpp` + `superpowers:test-driven-development` + `verification-before-completion` | Daisy Field firmware as a USB MIDI controller for DaisyHost | `5/5` | Kept the implementation small and firmware-appropriate: a new controller-only Field target uses libDaisy USB MIDI, avoids an audio callback, and relies on DaisyHost's existing MIDI learn/tracker path instead of inventing a host protocol. | `5/5` | Red pytest failed because `field/DaisyHostController` did not exist; green pytest passed `1/1`; `make` from `field/DaisyHostController` passed with FLASH `98252 B` / `74.96%`; QAE passed with `0 error(s), 0 warning(s)` after rerunning with `PYTHONIOENCODING=utf-8`. | Hardware flashing, USB MIDI enumeration, DaisyHost MIDI learn, and DAW/VST3 routing are still manual validation gaps. The QAE script needs UTF-8 output in this Windows shell. | Reuse this pattern for narrow firmware utility targets: red file-contract test, compile firmware, run QAE, then stop before claiming hardware behavior. |
| 2026-04-26 | `brainstorming` + `superpowers:test-driven-development` + `verification-before-completion` | DaisyHost Field CV target menu and generator visibility refinement | `5/5` | Kept the UI request anchored in a testable mapping contract first: default and alternative K layouts must not overlap, and the CV dropdown must expose both safe sets with `.1`/`.2` labels before editor layout changes are trusted. | `5/5` | Targeted Debug CTest passed `66/66`; Debug `DaisyHostPatch_Standalone` built; full `cmd /c build_host.cmd` passed Release `ctest` `187/187`. | The harness still does not capture a Field UI screenshot, so visual polish is build/smoke-backed but not screenshot-backed. | Reuse for host-board UI refinements where visual requests have a small pure mapping contract underneath. |
| 2026-04-26 | `systematic-debugging` + `superpowers:test-driven-development` + `verification-before-completion` + `daisy-cpp` | DaisyHost Field CV generator stability | `5/5` | Kept the bug at the mapping/runtime boundary instead of changing DSP: the observed disappearing audio was treated as a CV routing/modulation problem until tests pinned unsafe targets and double application. | `5/5` | Red Debug build failed because `ShouldForwardDaisyFieldCvInput` did not exist; targeted Debug CTest passed `64/64`; full `cmd /c build_host.cmd` passed Release `ctest` `185/185`. | This host-side pass does not replace hands-on standalone audio validation with CloudSeed/MultiDelay CV generators. | Reuse for CV/control bugs where one UI gesture may write both virtual ports and parameters. |
| 2026-04-26 | `brainstorming` + `superpowers:test-driven-development` + `systematic-debugging` + `daisy-cpp` + `verification-before-completion` | DaisyHost Field UI/control separation and CloudSeed safe advanced page | `5/5` | The approved prompt already supplied the design intent, so brainstorming stayed to scope confirmation. TDD pinned the separation and CloudSeed safety behavior before implementation; systematic debugging kept the old shared X/C navigation from being treated as an app bug; verification forced a full Release host gate after the JUCE editor changes compiled. | `5/5` | Red Debug build failed on missing CV target helper, CloudSeed page fields, and Field knob override bindings; green targeted Debug CTest passed `59/59`; full `cmd /c build_host.cmd` passed Release `ctest` `183/183`. | MP4 inspection was unavailable because local video tooling was missing; no firmware/hardware validation applied because this pass was host UI/control only. | Reuse this combination for host-board UI behavior changes that need app-menu, drawer, and parameter-mapping contracts kept separate. |
| 2026-04-26 | `brainstorming` + `superpowers:test-driven-development` + `verification-before-completion` | CloudSeed arpeggiator parameter-stepper | `5/5` | Kept the feature aligned with repo evidence: CloudSeed stayed an audio-input effect and gained deterministic effective-state parameter stepping rather than MIDI-note synthesis. | `5/5` | Red Debug payload first failed for missing `arp_*` canonical parameters, missing `node0/menu/arp/*`, and render rejecting the arp menu id; green Debug and Release payload filters passed `13/13`; Release render smoke passed with `cloudseed_smoke.json` exercising the arp path. | Full wrapper gate did not reach Release `ctest` because a running `DaisyHost Hub.exe` process locked the Release Hub executable during link. | Reuse this pattern for performance-first effect features where the right product behavior is effective-state modulation, not a new synth/MIDI contract. |
| 2026-04-26 | `systematic-debugging` + `superpowers:test-driven-development` + `daisy-cpp` | SubharmoniqField audible-default no-audio follow-up | `5/5` | Avoided another surface-only fix by proving the internal-clock path was still too quiet in the host core, then preserving firmware startup defaults with pickup-style K1-K8 handling. | `5/5` | Red: tightened `SubharmoniqCoreTest.PlayToggleRunsInternalClockAndProducesAudio` failed with peak `0.00323891826` and energy `0.383889765`; green targeted Subharmoniq tests passed `6/6`, firmware `make` passed with FLASH `124520 B` / `95.00%`, QAE passed `0/0`, and `cmd /c build_host.cmd` passed `175/175`. | MP4 inspection was limited because `ffprobe` is not installed; hardware flash/manual audio validation was not run. | Reuse for audio bugs where “nonzero output” may still be musically unusable. |
| 2026-04-26 | `systematic-debugging` | SubharmoniqField no-audio root-cause fix | `5/5` | Traced the silent behavior to the portable core transport path: `play_toggle` set `playing`, but no internal clock advanced rhythms inside `Process`, so the Field adapter could stay silent without external MIDI clock or Gate In. | `5/5` | Regression `SubharmoniqCoreTest.PlayToggleRunsInternalClockAndProducesAudio` failed red with zero triggers and zero audio, then passed after adding the internal tempo clock. | Hardware audio output is still not physically verified until `make program` and manual checks run. | Reuse for firmware/audio bugs before touching surface symptoms. |
| 2026-04-26 | `daisy-cpp` | SubharmoniqField audio and host Field surface refinement | `4/5` | Kept the firmware fix portable and real-time safe while aligning host Field controls with the hardware adapter: SW1/SW2 navigation, X/C shortcuts, K parameter labels, octave-visible keyboard, top I/O artwork, and CV/Gate trace cues. | `4/5` | `field/SubharmoniqField` `make` passed with FLASH `124424 B` / `94.93%`; QAE passed `0/0`; host targeted render/smoke rerun passed `3/3`; final wrapper gate passed `175/175`. | Automated UI smoke does not capture a screenshot in this harness; visual QA still needs a manual standalone run. | Reuse for paired firmware plus host-board-surface work. |
| 2026-04-26 | `superpowers:test-driven-development` | Subharmoniq portable core, hosted app, and Field firmware adapter | `5/5` | Kept the feature contract-first: the host build failed first on the missing Subharmoniq source, tests then drove the portable core/wrapper/registry behavior, and the firmware adapter had its own build-red compile fix before green. | `5/5` | Red host proof: `cmake --build build --config Debug --target unit_tests` failed on missing `src/DaisySubharmoniqCore.cpp`; targeted host CTest passed `20/20`; firmware `make` first failed on const `MidiEvent::AsNoteOn()` use, then passed. | Firmware TDD is still mostly build-contract proof until hardware is flashed and exercised. | Reuse for new portable-core plus firmware-adapter packages. |
| 2026-04-26 | `executing-plans` | Executing approved SubharmoniqField plan | `4/5` | Kept the work aligned with the approved Round 1 scope: portable core first, full hosted-app registration, Field adapter, SW1/SW2 pseudo-encoder navigation, and Round 2 filter reminder without implementing ladder/BPF yet. | `4/5` | Delivered the planned core/wrapper/adapter/docs/tests and preserved explicit caveats for unflashed hardware and deferred Round 2 filters. | The plan assumed a clean environment, but the checkout already had broad dirty state and the direct Debug host rebuild later hit the known duplicate `Path` / `PATH` MSBuild issue. | Reuse for approved synth/firmware plans, with dirty-worktree and wrapper-gate cautions. |
| 2026-04-26 | `daisy-cpp` | Daisy Field firmware adapter for a new portable synth core | `4/5` | Kept audio processing in the callback and Field controls, MIDI, CV/Gate, LEDs, and OLED in the main loop, following the existing Field adapter pattern. | `4/5` | `field/SubharmoniqField` `make` passed with FLASH `124176 B` / `94.74%`; QAE passed `0 error(s), 0 warning(s)`. | The skill cannot validate actual audio/CV/MIDI hardware behavior without flashing and hands-on checks. | Reuse for Field firmware adapters and hardware-facing follow-ons. |
| 2026-04-26 | `verification-before-completion` | Subharmoniq hosted-app and firmware closeout | `5/5` | Prevented overclaiming: the feature is host-gate green and firmware build/QAE-verified, but not flashed or hardware-validated. | `5/5` | Targeted host CTest passed `20/20`; `field/SubharmoniqField` `make` passed; QAE passed `0/0`; `cmd /c build_host.cmd` passed with Release `ctest` `173/173`. | Direct Debug host rebuild still needs wrapper/sanitized environment in this shell; manual hardware validation remains pending. | Reuse for every hosted-app plus firmware closeout. |
| 2026-04-25 | `superpowers:test-driven-development` | PolyOsc hosted-app import | `5/5` | Kept the import contract-first: the red build failed on missing `DaisyPolyOscCore` and `apps::PolyOscCore`, then implementation moved through portable core, hosted adapter, registry, Field mapping, render tests, and smoke coverage. | `5/5` | Red `cmake --build build --config Debug --target unit_tests` failed on missing PolyOsc headers; focused Debug payload passed `11/11`; full wrapper gate passed `168/168`. | The first focused filter had no PolyOsc-named Field/render tests until they were added explicitly, so future hosted-app plans should name filter-matching tests up front. | Reuse for additional hosted-app imports. |
| 2026-04-25 | `verification-before-completion` | PolyOsc hosted-app closeout | `5/5` | Prevented claiming Field firmware or hardware parity: PolyOsc is verified as a host-side DaisyHost app with Patch and Field board-surface coverage only. | `5/5` | Verification included focused Debug payload `11/11`, sanitized Release `DaisyHostRender` build, render smoke including PolyOsc and Field PolyOsc scenarios, and `cmd /c build_host.cmd` with Release `ctest` `168/168`. | Raw Release target build still hit the known duplicate `Path` / `PATH` MSBuild issue before the sanitized rerun. | Reuse for hosted-app closeouts and host-only board-surface work. |
| 2026-04-25 | `superpowers:test-driven-development` | HW/App adapter generator and audit tooling | `5/5` | Kept the tooling contract-first: tests pinned generated Makefile/source/docs fragments and audit classifications before the generator or audit script existed. | `5/5` | Red proof: pytest failed because `tools/generate_field_adapter.py` and `tools/audit_firmware_portability.py` did not exist. Green proof: pytest passed `3/3`, generated firmware `make` passed, QAE passed `0 error(s), 0 warning(s)`, targeted Debug CTest passed `52/52`, and the full host gate passed `159/159`. | Pytest's default temp/cache locations were blocked in this shell, so the tests use repo-local `.tmp` work dirs and the verification command disables the cache provider. | Reuse for DaisyHost Python tooling and golden-output generators. |
| 2026-04-25 | `executing-plans` | Executing HW/App adapter pipeline plan | `4/5` | Kept the sprint to the requested semi-automatic scope: generator, spec, generated MultiDelay proof, and read-only firmware audit, without changing DaisyHost rack/runtime or claiming arbitrary source translation. | `4/5` | Delivered `tools/generate_field_adapter.py`, `tools/adapter_specs/field_multidelay.json`, `tools/audit_firmware_portability.py`, tests, and generated `field/MultiDelayGenerated`; recorded firmware build/QAE and host-gate evidence. | The generated adapter was not flashed because the plan marked flash optional and flashing is a hardware side effect. | Reuse for bounded generator/tooling plans with explicit non-goals. |
| 2026-04-25 | `daisy-cpp` | Generated Field firmware adapter pipeline | `4/5` | Kept generated output firmware-shaped: `DaisyField`, main-loop controls, OLED/LED updates outside the callback, `MultiDelayCore` SDRAM delay storage, and `OPT = -Os` for Field flash fit. | `4/5` | `field/MultiDelayGenerated` built with FLASH `121376 B` / `92.60%` and passed QAE `0/0`. The audit tool classified both hand-written and generated Field adapters as `portable-core-ready`. | Hardware flash and manual audio/CV validation were not run for the generated adapter. | Reuse for future shared-core to board-firmware generators. |
| 2026-04-25 | `verification-before-completion` | Adapter pipeline closeout | `5/5` | Prevented overclaiming: the pipeline is source-backed, pytest-backed, generated-firmware build/QAE-backed, and host-gate-backed, while generated-adapter flash/manual validation remain open. | `5/5` | Verification included pytest `3/3`, generator run, generated firmware `make`, QAE `0/0`, audit reports, targeted Debug CTest `52/52`, and `cmd /c build_host.cmd` with Release `ctest` `159/159`. | Generated-adapter flash is explicitly not verified. | Reuse for every generator or firmware-pipeline closeout. |
| 2026-04-25 | `daisy-cpp` | First Daisy Field firmware adapter | `4/5` | Helped keep the adapter idiomatic for Daisy hardware: `DaisyField`, main-loop control scanning, real-time-safe audio callback, SDRAM delay allocation, and ST-Link/programming expectations stayed explicit. | `4/5` | `field/MultiDelay` now builds, QAE validation passes `0 error(s), 0 warning(s)`, and `make program` detected STLINK `V3J7M2` then reported `** Verified OK **`. | The skill cannot prove audio/CV behavior without physical hands-on validation; that checklist remains pending. | Reuse for Field firmware parity work and hardware-facing adapters. |
| 2026-04-25 | `superpowers:test-driven-development` | Field firmware adapter red/green scaffold | `5/5` | Kept the sprint honest by adding the Makefile first, proving the missing-source failure, then implementing only the adapter needed to make the firmware build pass. | `5/5` | Red proof: `make` from `field/MultiDelay` failed with `No rule to make target 'build/MultiDelay.o'`. Green proof: `make` passed after implementing `MultiDelay.cpp` and adding `OPT = -Os`. | Firmware red tests are build-scaffold checks rather than unit tests; manual behavior still needs hardware checklist evidence. | Reuse for firmware examples where build wiring is the first contract. |
| 2026-04-25 | `executing-plans` | Sprint F3 plan execution | `4/5` | Kept implementation limited to the requested first adapter, docs, QAE, flash, and host regression checks without reopening DaisyHost rack/runtime behavior. | `4/5` | Delivered `field/MultiDelay/Makefile`, `MultiDelay.cpp`, `README.md`, and `CONTROLS.md`; recorded build, QAE, ST-Link, Debug targeted CTest `52/52`, and full host gate `159/159`. | The plan's "hardware validated" definition includes manual audio/control/CV checks that the assistant cannot physically observe; closeout must stop at flash-verified until those are run. | Reuse for approved DaisyHost/firmware sprints with explicit out-of-scope boundaries. |
| 2026-04-25 | `verification-before-completion` | Field firmware adapter closeout | `5/5` | Prevented overclaiming: the adapter is build-verified and ST-Link flash-verified, while full hardware validation remains pending until the manual checklist is run. | `5/5` | Verification included firmware `make`, QAE `0/0`, `make program` with `** Verified OK **`, targeted Debug CTest `52/52`, and `cmd /c build_host.cmd` with Release `ctest` `159/159`. | Physical audio, knob, CV, LED, OLED, and dropout checks still require hands-on confirmation on the connected Field. | Reuse for every firmware or hardware-facing closeout. |
| 2026-04-25 | `superpowers:test-driven-development` | Field ergonomics polish and board-generic UI cleanup | `5/5` | Kept the F1/F2 sprint contract-first: mapping tests failed before `knobParameterIds` existed, and board-profile tests failed before Field surface controls exposed target ids. | `5/5` | Red proof: Debug `unit_tests` build failed on missing `HostedAppPatchBindings::knobParameterIds`; Lane B `BoardProfileTest` failed `10/11` on missing target ids. Green proof: targeted Debug CTest passed `70/70`, Release standalone/render smoke passed, and `cmd /c build_host.cmd` passed `159/159`. | The worker could add useful red coverage but the production fix crossed profile/editor integration, so the main thread had to own the final join. | Strong reuse for Field mapping and board-profile cleanup packages. |
| 2026-04-25 | `dispatching-parallel-agents` | Parallel F1/F2 lane split | `4/5` | Helped run a bounded Lane B board-profile test lane while the main thread worked on Lane A mapping and final integration. | `4/5` | Worker Lane B added red coverage for Field surface target lookup and Patch-only wording. The main thread then implemented profile target metadata and editor lookup, reran targeted tests, smoke, and the full gate. | The write set was close enough to editor/profile integration that only one worker was safe; more parallelism would have increased merge risk. | Reuse for one or two independent red-test/audit lanes, not for tightly coupled JUCE integration edits. |
| 2026-04-25 | `executing-plans` | Executing the combined Field F1/F2 sprint | `4/5` | Kept the sprint inside the requested boundary: Field ergonomics plus board-generic UI cleanup only, with `daisy_patch` default, rack topology, routing, DAW automation, firmware, and hardware scope unchanged. | `4/5` | The delivered package added explicit Patch-page knob parameter metadata, updated Field K5-K8 mapping exclusions, added profile target metadata, and updated docs after a full `159/159` gate. | The plan named two parallel lanes, but the checkout's dirty shared state required a single integrator to do the production edit and final proof. | Reuse for scoped DaisyHost sprints that need manager-readable status and strict out-of-scope boundaries. |
| 2026-04-25 | `verification-before-completion` | Field ergonomics/UI cleanup closeout | `5/5` | Forced the final claim to rest on fresh evidence after code and docs changed, including targeted Debug tests, Release smoke, and the wrapper gate. | `5/5` | Verification included targeted Debug CTest `70/70`, Release standalone smoke for `daisy_patch` and `daisy_field`, Release render smoke including Field native/extended/selected-node scenarios, and `cmd /c build_host.cmd` with Release `ctest` passing `159/159`. | Raw Release target build still needs wrapper or `Path` / `PATH` sanitization in this shell. Manual DAW/VST3 and hardware validation remain intentionally outside this sprint. | Reuse for every Field or board-generic UI closeout. |
| 2026-04-25 | `systematic-debugging` | Board/app-independent knob-drag crash triage | `4/5` | Prevented a misleading app- or board-specific fix. The crash reproduced through the shared knob/control path, and inspection showed message-thread control refreshes could touch the same hosted app core while the audio thread was processing it. | `5/5` | Root cause was narrowed to concurrent hosted-core access, then fixed with processor-side serialization around core state and selected-node/core handoff. Visible Debug standalone validation survived knob dragging on both `daisy_field` and `daisy_patch`. | No crash dump was available, so diagnosis relied on source inspection, reproduction shape, and live process validation. | Reuse for JUCE runtime crashes, especially when symptoms appear app-specific but the event path is shared. |
| 2026-04-25 | `superpowers:test-driven-development` | Processor-adjacent knob-drag crash fix | `5/5` | The TDD workflow helped define the regression contract, but a true red automated test was not practical because the current unit harness does not exercise concurrent JUCE audio/message-thread knob dragging. | `3/5` | Verification instead used `cmake --build build --config Debug --target unit_tests`, targeted Debug CTest `66/66`, standalone Debug builds, visible Patch/Field knob-drag validation, Release smoke, and the full wrapper gate `155/155`. | Missing host-side stress harness for UI/audio-thread races. | Reuse with caution for processor/editor race fixes; add a dedicated stress harness before expecting red-first automation. |
| 2026-04-25 | `verification-before-completion` | Knob-drag crash fix closeout | `5/5` | Forced the fix to be verified beyond compilation: targeted Debug checks, visible standalone knob dragging for both boards, Release smoke, and the full wrapper gate all ran before the crash fix was written up. | `5/5` | `cmd /c build_host.cmd` passed with Release `ctest` `155/155`; standalone smoke passed for `daisy_patch` and `daisy_field`; render smoke passed; manual visible Debug standalone validation confirmed Patch and Field knob dragging stayed alive/responding. | Manual DAW/VST3 validation remains deferred, and the race does not yet have an automated red regression test. | Reuse for every DaisyHost runtime stability fix before claiming a crash is fixed. |
| 2026-04-25 | `writing-plans` | Field follow-on options organized into implementation sprints | `4/5` | Converted a loose list of possible Field refinements into sprint-sized packages with goals, likely files, red tests, implementation notes, verification commands, and explicit out-of-scope boundaries. | `4/5` | `FIELD_PROJECT_TRACKER.md` now separates F1 ergonomics, F2 board-generic UI cleanup, F3 hardware/firmware parity, and F4 automation expansion while keeping DAW/VST3 validation outside the implementation backlog. | The generic skill suggests saving to `docs/plans`, but the user explicitly asked for a persistent Field project tracker, so the output belongs in `FIELD_PROJECT_TRACKER.md`. | Reuse for future Field scope breakdowns where sprint boundaries matter more than a one-off plan file. |
| 2026-04-25 | `doc-coauthoring` | Field-specific tracker creation and cross-doc linking | `3/5` | Helped keep the new Field tracker reader-oriented: current state first, validation todo separated from implementation work, then sprint backlog and out-of-scope rules. | `3/5` | `PROJECT_TRACKER.md` and `WORKSTREAM_TRACKER.md` now point readers to `FIELD_PROJECT_TRACKER.md` for Field follow-on work. | The full collaborative question loop would be too heavy for this direct maintenance request, so only the structure and reader-testing parts were applied. | Reuse the lightweight variant for dedicated subtrack trackers. |
| 2026-04-25 | `superpowers:test-driven-development` | Field refinement closeout for startup-request launch planning and selected-node render evidence | `5/5` | Kept the refinement honest: Hub tests failed first while Play/Test used command-line args only, and the render test failed first while Field surface manifests still reported `node0` for a selected `node1` scenario. | `5/5` | Red proof: `ctest --test-dir build -C Debug --output-on-failure -R "(HubSupportTest|RenderRuntimeTest)"` failed on the intended Hub/render gaps. Green proof: focused Debug CTest passed `39/39`, processor snapshot/helper follow-up passed `42/42`, broader targeted Debug passed `77/77`, Release standalone/render smoke passed, and `cmd /c build_host.cmd` passed `155/155`. | The implementation stayed narrow, but raw Release MSBuild still hit the known duplicate `Path` / `PATH` shell issue until rerun with a sanitized environment or through the wrapper. | Strong reuse for refinement packages where the visible product gap is proof/wording rather than a new runtime feature. |
| 2026-04-25 | `executing-plans` | Executing the Field refinement closeout plan as one workpackage | `4/5` | Helped keep the workpackage bounded to selection proof, selected-node Field evidence, board-aware UI copy, and docs, without reopening topology, DAW automation, firmware, or hardware scope. | `4/5` | The delivered package touched Hub planning, render manifest selection, one UI copy branch, smoke validation, and tracking docs while preserving `daisy_patch` default and `LiveRackTopology` behavior. | The generic workflow suggests separate worktrees and many checkpoints; this checkout's shared dirty state made a single integrator path safer than writable parallel workers. | Reuse for one-sprint DaisyHost closeouts with explicit scope boundaries and final gate requirements. |
| 2026-04-25 | `verification-before-completion` | Field refinement closeout verification | `5/5` | Forced fresh evidence before claiming the refinement complete: targeted Debug checks, Release smoke, and the wrapper host gate all ran after implementation and docs updates. | `5/5` | Verification included targeted Debug CTest (`77/77`), Release standalone smoke for `daisy_patch` and `daisy_field`, Release render smoke including `field_node_target_surface_smoke.json`, and `cmd /c build_host.cmd` with Release `ctest` passing `155/155`. | Raw Release build still needs wrapper/sanitized environment in this shell; manual DAW/VST3 and hardware validation remain intentionally outside this sprint. | Reuse for every DaisyHost closeout that updates manager-facing status or claims Field readiness. |
| 2026-04-25 | `subagent-driven-development` | Field refinement closeout audit and review support | `4/5` | Kept parallelism low-risk: prior audit lanes helped identify the smallest useful sprint, and a read-only review pass caught the processor snapshot node-id gap before final verification. | `4/5` | The integrator fixed the review finding by adding a shared Field CV OUT port-id helper, then reran focused Debug `42/42`, broader targeted Debug `77/77`, Release smoke, and the `155/155` full wrapper gate. | Writable parallel workers were avoided during final integration because the checkout already had broad dirty state; read-only review provided the best value-to-risk ratio. | Reuse read-only subagent review near closeout when the package spans processor, render, smoke, and docs but the implementation write set should stay centralized. |
| 2026-04-25 | `superpowers:test-driven-development` | Field extended host surface support on top of the landed Field shell/native controls | `5/5` | Kept the sprint contract-first: Field CV OUT, SW, LED metadata, mapping, snapshot, render manifest, and smoke expectations were added as failing tests before implementation. | `5/5` | Red proof: `cmake --build build --config Debug --target unit_tests` failed on missing Field output/switch/LED bindings, profile indicator metadata, snapshot fields, and render manifest state. Green proof: targeted Debug CTest passed `68/68`, Release standalone/render smoke passed, and `cmd /c build_host.cmd` passed `152/152`. | One smoke failure exposed a scenario-order issue: SW2 maps to `Init Patch`, so K5/K6 needed to be driven after SW utility actions to keep CV OUT evidence stable. | Strong reuse for board-surface packages that require mapping, UI, render, smoke, and docs to agree. |
| 2026-04-25 | `verification-before-completion` | Field extended host surface closeout | `5/5` | Forced the final claim to be exactly scoped: host-side Field outputs/switches/LEDs are verified, while Field firmware, real hardware voltage output, mixed-board racks, and manual DAW/VST3 validation remain deferred. | `5/5` | Verification included targeted Debug CTest (`68/68`), Release standalone smoke for `daisy_patch` and `daisy_field`, Release render smoke including `field_extended_surface_smoke.json`, and the full wrapper gate (`152/152`). | Raw Release `cmake --build` initially hit the duplicate `Path` / `PATH` shell issue; the checked-in wrapper gate remained green. | Reuse for every DaisyHost board/runtime closeout where docs must distinguish automated host proof from hardware or DAW claims. |
| 2026-04-25 | `daisy-cpp` | Field hardware vocabulary applied to host-side Field outputs/switches/LEDs | `4/5` | Kept the package honest: CV OUT 1-2 are host-visible derived monitor outputs, SW1/SW2 are host momentary utility triggers, and LEDs are derived indicators rather than firmware or real hardware output claims. | `4/5` | The implemented Field extended host surface matches the requested host-plus-outputs scope and leaves firmware, real hardware voltage output, and hardware-specific ergonomics out of the landed claim. | DaisyHost-local source contracts still determined the actual mapping and test surfaces. | Reuse for Field firmware/hardware follow-ons, especially when separating host simulation from real board behavior. |
| 2026-04-25 | `superpowers:test-driven-development` | Daisy Field native controls on top of the landed board-support shell | `5/5` | Kept the sprint contract-first: `BoardControlMapping`, session persistence, render `surface_control_set`, and frozen-rack regression tests landed before the processor/editor Field surface was treated as complete. | `5/5` | Red proof included the Debug build failing on missing `daisyhost/BoardControlMapping.h`, then later failing on missing `RenderTimelineEventType::kSurfaceControlSet` / `RenderTimelineEvent::controlId`; green proof passed the targeted Debug CTest subset (`57/57`), Release standalone/render smoke, and the full wrapper gate (`144/144`). | Dirty worktree state still required careful source-truth checks so the red evidence stayed tied to this checkout rather than a clean branch diff. | Strong reuse for host-side board-control packages where mappings, persistence, render schema, and UI behavior all need to agree. |
| 2026-04-25 | `executing-plans` | Implementing the Field native controls sprint without reopening rack topology | `4/5` | Helped keep the implementation within the requested package boundary: host-side K1-K8, CV1-CV4, Gate In, and A1-B8 mappings only, with `daisy_patch` default and rack topology unchanged. | `4/5` | The delivered package added `BoardControlMapping`, Field processor/editor APIs, render `surface_control_set` support, Field native controls smoke coverage, and docs without changing `LiveRackTopology` or mixed-board rack semantics. | The plan needed active correction for landed shell state and existing dirty files, but the package boundary stayed useful. | Reuse for future DaisyHost implementation sprints that have explicit "do not touch" boundaries. |
| 2026-04-25 | `verification-before-completion` | Daisy Field native controls closeout | `5/5` | Forced the claim at that checkpoint to separate landed host-side Field native controls from still-deferred Field firmware, real hardware output, mixed-board racks, and manual DAW/VST3 validation. | `5/5` | Verification included targeted Debug CTest (`57/57`), Release standalone smoke, Release render smoke with Field shell and Field native controls scenarios, and `cmd /c build_host.cmd` with Release `ctest` passing `144/144`. Later same-day work added host-side CV OUT monitor outputs, SW1/SW2, and LEDs. | No firmware `make` rerun was required because this package stayed host-side; manual DAW/VST3 validation remains intentionally deferred. | Reuse for every DaisyHost closeout that touches board/runtime behavior or smoke coverage. |
| 2026-04-25 | `daisy-cpp` | Field hardware vocabulary applied to host-side Field native controls | `4/5` | Kept the host mapping aligned with real Field affordances while avoiding a firmware implementation: knobs, CV inputs, Gate In, and keys became host inputs, then later same-day extended-surface work added host-side CV OUT monitor outputs, SW1/SW2, and LEDs. | `4/5` | The resulting mapping covers K1-K8, CV1-CV4, Gate In, A1-B8 MIDI keys, CV OUT monitor outputs, switches, and LED evidence as host-side behavior only. | The skill does not decide DaisyHost runtime behavior by itself; local processor, render, and session contracts remained the authority. | Reuse for future Field firmware or hardware-output follow-ons. |
| 2026-04-24 | `superpowers:test-driven-development` | Daisy Field board-support shell through the board factory seam | `5/5` | Kept the package contract-first and bounded: Field support started with board/profile, Hub, session/render, smoke, and frozen-rack assertions before implementation changed the factory, processor, editor, and smoke harness. | `5/5` | Red proof included `cmake --build build --config Debug --target unit_tests` failing on the active Field profile contract before `ControlKind::kKey` / `kSwitch` and `MakeDaisyFieldProfile` existed in the working tree, plus the targeted Hub test failing because non-default board render planning reused the default example. Green proof passed the Debug target, the focused `ctest` subset (`54/54`), and the full wrapper host gate (`136/136`). | Some Field contract declarations already existed in `HEAD` while the working tree was dirty, so the red evidence had to be interpreted against the current checkout state rather than file diffs alone. | Strong reuse for board-support packages where the feature boundary matters as much as the implementation. |
| 2026-04-24 | `daisy-cpp` | Daisy Field hardware vocabulary applied to a host-side passive board shell | `4/5` | Kept the Field profile tied to actual board affordances while avoiding a firmware-style implementation inside DaisyHost. | `4/5` | The Field shell models eight knobs, four CV inputs, two CV outputs, gate in/out, stereo audio I/O, OLED, keys, switches, and LED affordances as board metadata only; no Field controls were bound to hosted-app parameters. | The skill still needs DaisyHost-local judgment because host board metadata is not the same as libDaisy firmware control behavior. | Reuse for future Field hardware follow-ons, while keeping shell metadata separate from live host control mappings. |
| 2026-04-24 | `executing-plans` | Implementing the refined frozen-rack Field board-support shell plan | `4/5` | Helped keep the implementation aligned with the controlling rule: treat the rack as frozen and add Field as a separate board-support package through the factory seam. | `4/5` | The delivered package touches `BoardProfile`, Hub board selection/planning, board-id-safe processor/render handling, passive editor drawing, and smoke/docs without expanding `LiveRackTopology`, routing, selected-node behavior, or hosted-app DSP semantics. | The existing dirty checkout meant the plan had to be executed with careful source-truth checks and no unrelated cleanup. | Reuse for future DaisyHost implementation plans that need explicit "do not touch" boundaries. |
| 2026-04-24 | `verification-before-completion` | Daisy Field board-support shell closeout | `5/5` | Forced the final status to stay at "Field board-support shell" and not drift into Field native controls claims before that separate package existed. | `5/5` | Verification included the Debug unit-test build, targeted Debug CTest for board/profile/Hub/session/render/effective-state coverage (`54/54`), and the full wrapper host gate with `ctest` passing `136/136`, including standalone/render smoke. | Manual DAW/VST3 load validation remains deferred under the existing project policy; no firmware `make` rerun was required because this package stayed host-side. | Reuse for every DaisyHost closeout that touches board metadata or smoke coverage. |
| 2026-04-24 | `writing-plans` | Daisy Field plan refinement around frozen-rack board-support scope | `4/5` | Refined `../docs/plans/2026-04-24-daisyhost-field-board.md` so the next package was a narrow Field board-support shell through the existing board factory seam, with rack topology, routing, hosted-app bindings, and Field native controls explicitly out of scope. | `4/5` | The revised plan had a controlling decision section, a package boundary, milestones limited to board profile/Hub/session/render/passive panel/smoke/docs, and prohibited completion wording for Field native controls claims. | The prior draft was too broad because it carried Field native control behavior into the same plan; the refinement needed an explicit "deferred follow-on" section. | Reuse when a DaisyHost implementation plan needs scope narrowing before code starts. |
| 2026-04-24 | `writing-plans` | Daisy Field board implementation plan after source-backed implementability verification | `4/5` | Produced `../docs/plans/2026-04-24-daisyhost-field-board.md` with the current gate status, source evidence, staged implementation milestones, exact file touch points, red/green test expectations, and final verification criteria. | `4/5` | The draft plan distinguished selectable Field shell support from Field native controls, and it kept the known full Release host-gate blocker as Milestone 0 instead of allowing implementation to start from partial evidence. | The generic plan format was heavier than a simple manager summary and needed DaisyHost-specific correction for the dirty worktree and Release payload blocker known at that time. | Reuse for future DaisyHost board plans where implementation should not begin until gate and scope are explicit. |
| 2026-04-24 | `daisy-cpp` | Daisy Field hardware surface verification for DaisyHost planning | `4/5` | Grounded the Field scope in local board evidence: 8 knobs, 4 CV inputs, 2 CV outputs, 16 key controls with LEDs, 2 switches, gate in/out, MIDI in/out, stereo audio I/O, and OLED. | `4/5` | Checked `../libDaisy/src/daisy_field.h`, `../field/README.md`, and recent Field project docs before writing the plan. | The skill does not answer host-side feasibility by itself; DaisyHost source inspection was still required to find the Patch-shaped arrays and Hub/Profile seams. | Reuse whenever DaisyHost board support depends on real Daisy hardware vocabulary. |
| 2026-04-24 | `superpowers:test-driven-development` | Harmoniqs + VA Synth hosted-app landing with shared-core, adapter, render tests, and smoke coverage | `5/5` | Forced a clean red/green path for the dual-app wave: missing app surfaces failed first, then the implementation moved through targeted Debug payload proof and Release render smoke before the docs were allowed to claim the apps had landed. | `5/5` | Red proof: registry expectation failed first, then `cmake --build build --config Debug --target unit_tests` failed on the missing Harmoniqs/VA Synth wrapper and app headers. Green proof: rebuilt Debug payload passed `18/18` through `tests/run_unit_test_payload.py`, and Release `tests/run_smoke.py --mode render` passed after the new scenarios were wired in. | The current `ctest -C Debug -R ...` path can still point at a stale run-tag payload in this checkout, so the direct payload runner remained necessary for targeted proof. | Strong reuse for future DaisyHost app waves where portable cores, hosted adapters, render tests, and checked-in smoke scenarios all land together. |
| 2026-04-24 | `verification-before-completion` | Harmoniqs + VA Synth closeout under the launcher constraints observed at that checkpoint | `5/5` | Prevented this pass from being overstated as fully gate-green. It forced the closeout to distinguish three layers clearly: red-step evidence existed, targeted Debug payload tests were green, Release render smoke was green, and the full Release `ctest` blocker was still external at that point. | `5/5` | The pass reran `cmake --build build --config Debug --target unit_tests`, executed the new targeted tests through `py -3 tests/run_unit_test_payload.py <fresh payload> --gtest_filter=...`, rebuilt `DaisyHostRender` in Release, and reran `py -3 tests/run_smoke.py --mode render --build-dir build --source-dir . --config Release --timeout-seconds 120`. | The full Release unit-test launcher was environment-blocked during that pass, so the closeout had to stop at targeted-Debug-payload-backed plus Release-render-smoke-backed instead of a fully green host gate. | Reuse whenever DaisyHost can prove a new feature through targeted unit/render evidence but the final aggregate gate is blocked by a machine-level launcher issue. |
| 2026-04-24 | `subagent-driven-development` | Parallel Harmoniqs/VASynth red-step scaffolding | `4/5` | Helped front-load the dual-app pass by splitting the first test scaffolds into two lanes while the main thread kept the critical path on local implementation and integration. | `3/5` | Two workers produced the initial Harmoniqs and VA Synth red tests in parallel; the main thread then implemented both apps locally, integrated registry/build/smoke wiring, and reran verification. | The worker results were useful but limited to red-phase scaffolding, so the main thread still had to own all production code and final proof. | Reuse when DaisyHost has two independent app surfaces that can benefit from parallel red-test setup without duplicating the implementation work. |
| 2026-04-24 | `doc-coauthoring` | Rack-freeze mitigation closeout across tracker/checkpoint/readme/changelog | `3/5` | Helped keep the refined blocker state aligned once the wrapper, payload path, launcher, and verification wording all changed in one iteration. | `3/5` | The closeout pass synchronized `PROJECT_TRACKER.md`, `CHECKPOINT.md`, `README.md`, `CHANGELOG.md`, and `SKILL_PLAYBOOK.md` around the new `build/unit_test_bin/<run-tag>/.../DaisyHostTestPayload.bin` path, the wrapper-injected run tag, and the fact that Release smoke passed while `110/112` Release tests still failed on payload `PermissionError`. | The workflow is still heavier than a simple one-file note, but the state drift risk was high because both workflow and blocker wording changed together. | Reuse whenever DaisyHost blocker state changes across multiple local truth docs at once. |
| 2026-04-24 | `verification-before-completion` | Rack-freeze mitigation after the wrapper/payload changes | `5/5` | Prevented treating the improved build stage as success once the wrapper reached full Release build plus smoke. It forced the closeout to stop at the real evidence line for that pass: `ctest -C Release` still failed `110/112` because the launcher could not read the freshly linked payload. | `5/5` | `cmd /c build_host.cmd` rebuilt the Release target set with a fresh run tag, `DaisyHostStandaloneSmoke` and `DaisyHostRenderSmoke` passed, and the remaining failures were logged as `PermissionError` from `tests/run_unit_test_payload.py` against `build/unit_test_bin/<run-tag>/Release/DaisyHostTestPayload.bin`. | The blocker was external to normal repo control, so the skill could hold the truth line but could not clear the machine-level read lock by itself. | Reuse whenever a DaisyHost verification pass improves materially but still cannot cross the final gate. |
| 2026-04-24 | `systematic-debugging` | Rack-freeze mitigation after the original runner-path workaround | `4/5` | Narrowed the blocker again instead of stopping at "still broken": it proved the old failure started as an exact-name/path denial in `test_bin`, proved the link-stage `LNK1104` could be removed with a new name/path, proved the final Release app target set could build again, and then isolated the remaining failure to a machine-level read lock on the freshly linked payload itself. | `5/5` | Exact-name write probes denied only `DaisyHostUnitTestsRunner.exe`; scratch-directory checks allowed `DaisyHostUnitTests.exe`; the wrapper with `DAISYHOST_UNIT_TEST_RUN_TAG` rebuilt the full Release target set; `ctest -C Release` then failed only when `tests/run_unit_test_payload.py` tried to read `DaisyHostTestPayload.bin`. | The debugging path required multiple mitigation attempts because the observed symptom changed from link failure to launch failure to read failure as each lower-level blocker was removed. | Strong reuse for DaisyHost toolchain failures where the visible symptom mutates as deeper blockers are peeled away. |
| 2026-04-24 | `systematic-debugging` | Braids verification hardening and Release runner launch failure | `4/5` | Split the resumed failure into separate causes instead of treating it as one generic "tests blocked" problem: wrapper PATH handling was broken outside the sandbox, the moved unit-test runner needed explicit output-directory creation, the Release runner still fails at CTest launch, and the Debug unit runner exposed one real Braids render-test duration bug. | `5/5` | `cmd /c build_host.cmd` first failed before CMake due `Env:PATH`; after the wrapper fix it rebuilt Release targets but CTest could not launch the Release runner; `ctest -C Debug -R "(AppRegistryTest|BraidsCoreTest|DaisyBraidsCoreTest|RenderRuntimeTest)"` initially failed `RenderRuntimeTest.ProducesDeterministicOfflineRenderForBraids`; after the duration fix, the same focused run passed `27/27` and the full Debug unit set passed `110/110`. | The Release runner launch issue remains unresolved, so the final proof has to stay Debug-unit-backed plus Release-smoke-backed. | Strong reuse for DaisyHost verification problems where build-tool behavior and real test failures are interleaved. |
| 2026-04-24 | `systematic-debugging` | Rack freeze resume and Daisy Field gating check | `4/5` | Narrowed the remaining blocker from a vague "Release CTest fails" into a specific machine-level runner-path problem: the wrapper fix is good, `probe.exe` creation succeeds, copied non-test executables run from `build/test_bin/Release`, but the Release `DaisyHostUnitTestsRunner.exe` is still held open immediately after link and can later disappear entirely. | `5/5` | `.\build_host.cmd` now reaches configure/build; direct `probe.exe` creation in `build/test_bin/Release` passed; direct runner launch failed with "file is being used by another process"; copied `DaisyHostRender.exe` executed successfully from the same directory; attempted Defender exclusion failed due insufficient permissions. | The remaining blocker is outside normal repo control, so the workflow could diagnose it precisely but not clear it from this session. | Strong reuse whenever DaisyHost has to distinguish a repo/build-system failure from a machine-level security or file-lock issue. |
| 2026-04-24 | `verification-before-completion` | Rack freeze gating before Daisy Field implementation | `5/5` | Prevented an incorrect jump into `daisy_field` work before the gate was green. It forced that session to stop at the real gate: Release `ctest` was not green then, so the explicit Daisy Field readiness sentence could not be emitted yet. | `5/5` | The session reran the canonical wrapper, rebuilt the Release target set, proved that only the Release runner path remained blocked, and then held the line on not starting Daisy Field implementation. | This skill can only preserve accuracy; it cannot remove an external machine-level blocker by itself. | Reuse whenever a tempting next feature is gated behind one unresolved verification leg. |
| 2026-04-24 | `verification-before-completion` | Braids closeout after resumed verification | `5/5` | Forced a more precise closeout than the paused state: Release product binaries and smoke passed, Debug unit tests passed, and the remaining Release CTest failure stayed explicitly documented instead of being flattened into either success or failure. | `5/5` | `cmake --build build --config Debug --target unit_tests` passed; focused Debug `ctest` passed `27/27`; full Debug unit `ctest` excluding smoke passed `110/110`; Release `py -3 tests/run_smoke.py --mode all --build-dir build --source-dir . --config Release --timeout-seconds 120` passed. | Full Release `ctest` could not launch `build/test_bin/Release/DaisyHostUnitTestsRunner.exe` in that pass. | Reuse for all DaisyHost closeouts where one verification leg is blocked but other meaningful evidence is available. |
| 2026-04-24 | `doc-coauthoring` | Cross-doc verification-truth update after resumed Braids pass | `3/5` | Kept the tracker, checkpoint, changelog, README, and skill playbook aligned around the new `build/test_bin/<config>` runner path, the Debug `110/110` result, the Release smoke result, and the remaining Release CTest blocker. | `3/5` | `PROJECT_TRACKER.md` gained a 2026-04-24 ledger row; `CHECKPOINT.md` records the current verification split; `CHANGELOG.md` records wrapper/test-runner updates; `README.md` records the new runner path. | The docs remain more verbose than ideal because they have to preserve historical proof states from several same-week passes. | Reuse when DaisyHost verification status changes across multiple files. |
| 2026-04-23 | `systematic-debugging` | Braids host integration and unit-test-runner output instability | `4/5` | Prevented a wrong-layer fix during Braids bring-up by separating three failures that looked related at first glance: vendored Braids would not build under MSVC because of upstream `stmlib` attributes, the workspace already had a pre-existing `RenderRuntime.cpp` declaration gap, and the final `unit_tests` executable still had an environment-level lock/disappearance problem after link. | `5/5` | DaisyHost-local `include/stmlib/...` shims restored Braids compilation without editing the shared repo `stmlib/`; a minimal forward declaration unblocked the existing render-runtime compile drift; later build/smoke verification still had to be recorded as partial because `DaisyHostUnitTestsRunner.exe` could not be executed reliably in this shell. | The diagnostic path was longer than a quick one-off fix because each suspected cause had to be isolated before editing. | Strong reuse whenever DaisyHost host-toolchain compatibility and existing workspace drift can masquerade as one new feature failure. |
| 2026-04-23 | `superpowers:test-driven-development` | Braids hosted-app pilot with portable core, adapter, and smoke coverage | `5/5` | Forced the Braids package to start with a real red step: the first `unit_tests` build failed on the missing Braids wrapper/app surfaces, then the implementation was driven until the new core/app/render slice went green enough to rebuild the app targets and pass the smoke harness. | `5/5` | Red build failed first for the missing Braids surfaces; green verification rebuilt `unit_tests`, `DaisyHostHub`, `DaisyHostRender`, `DaisyHostPatch_VST3`, and `DaisyHostPatch_Standalone`; `ctest --test-dir build -C Release --output-on-failure -R "DaisyHost(Standalone|Render)Smoke"` passed `2/2`; `py -3 tests/run_smoke.py --mode all --build-dir build --source-dir . --config Release --timeout-seconds 120` passed with `braids_smoke.json` included. | The final green leg still could not include direct unit-test execution because the rebuilt runner output remained unstable in this shell. | Strong reuse for DaisyHost hosted-app pilots where a portable shared core, an app adapter, and smoke coverage can all be pinned down by red/green steps before declaring product value. |
| 2026-04-23 | `verification-before-completion` | Braids closeout with a partially blocked full-gate rerun | `5/5` | Prevented the Braids pass from being written up as fully green once the build targets and smoke harness succeeded; it forced the tracker/checkpoint/changelog closeout to stay explicitly at build-backed and smoke-backed while the runner lock persisted. | `5/5` | The closeout reran `cmake -S . -B build`, rebuilt `unit_tests`, `DaisyHostHub`, `DaisyHostRender`, `DaisyHostPatch_VST3`, and `DaisyHostPatch_Standalone`, reran `DaisyHostStandaloneSmoke` and `DaisyHostRenderSmoke` (`2/2`), and ran `py -3 tests/run_smoke.py --mode all --build-dir build --source-dir . --config Release --timeout-seconds 120` before documenting the remaining `DaisyHostUnitTestsRunner.exe` blocker. | The last unit-test execution leg was environment-blocked in that pass, so the closeout had to distinguish repo-backed proof from shell-specific failure. | Reuse whenever DaisyHost needs a precise "what is actually green" closeout instead of a vague partial-pass summary. |
| 2026-04-23 | `doc-coauthoring` | Braids cross-doc closeout after build and smoke verification | `3/5` | Helped keep the new Braids hosted-app status, checked-in smoke scenario, runner-name change, and partial-verification caveat aligned across `README.md`, `PROJECT_TRACKER.md`, `CHECKPOINT.md`, and `CHANGELOG.md`. | `3/5` | The same closeout pass synchronized the Braids app inventory, smoke-harness scope, and `DaisyHostUnitTestsRunner.exe` caveat without turning the partial verification into a full-green claim. | Still heavier than necessary for a one-file note, but appropriate once four tracking docs changed together. | Reuse when DaisyHost adds a new hosted app and the docs need to reflect both product scope and verification nuance at once. |
| 2026-04-23 | `subagent-driven-development` | Visible two-node rack sprint across helper, editor, and integrator lanes | `4/5` | Split the sprint into a topology-helper lane plus an editor/board-seam lane while the main thread owned the processor/session/render join. | `4/5` | Worker 1 delivered `LiveRackTopology` plus tests; Worker 2 delivered the board-factory seam and rack-header/editor slice; the main thread integrated the live processor/runtime and then rebuilt the app targets plus smoke coverage. | The final unit-test execution blocker still had to be diagnosed and documented centrally because it sat outside all three owned lanes. | Strong reuse for future DaisyHost packages with one or two isolated helper/UI lanes and one integrator-owned runtime lane. |
| 2026-04-23 | `superpowers:test-driven-development` | Visible rack topology, selected-node runtime targeting, and v5 session/render contract | `5/5` | Kept the sprint contract-first: the helper/tests defined legal topology shapes and rack fields before the processor/editor runtime was widened to two live nodes. | `5/5` | The sprint started with a targeted failing render-path check, then moved through rebuilt topology/session/render code, smoke verification, and direct forward/reverse `DaisyHostRender.exe` proofs for the serial two-node paths. | The final rebuilt `unit_tests` binary could not be executed in this shell because an external process held the file open after link, so the runtime proof had to fall back to smoke plus direct render evidence. | Strong reuse for DaisyHost runtime expansions where helper-layer route/state contracts can be specified before JUCE glue grows. |
| 2026-04-23 | `verification-before-completion` | Visible-rack sprint closeout with partial verification blocker | `5/5` | Prevented the sprint from being described as fully green once the app targets and smoke tests passed; it forced the docs to record that the final unit-test execution leg is still blocked by an external file lock in this shell. | `5/5` | The closeout reran `cmake -S . -B build`, rebuilt `unit_tests`, `DaisyHostHub`, `DaisyHostRender`, `DaisyHostPatch_VST3`, and `DaisyHostPatch_Standalone`, reran `DaisyHostStandaloneSmoke` and `DaisyHostRenderSmoke` (`2/2`), and ran direct forward/reverse two-node `DaisyHostRender.exe` proofs before documenting the remaining `DaisyHostUnitTestsRunner.exe` lock. | The full gate could not be completed against the final visible-rack code because the rebuilt unit-test executable was externally locked after link. | Reuse whenever DaisyHost closeout needs to distinguish repo regressions from environment-level blockers instead of flattening them into one vague failure. |
| 2026-04-23 | `doc-coauthoring` | Visible-rack cross-doc closeout after build/smoke/runtime proof | `3/5` | Helped keep the new live-rack status, board-seam staffing milestone, updated session/render versioning, and the partial-verification caveat aligned across `README.md`, `PROJECT_TRACKER.md`, `CHECKPOINT.md`, and `CHANGELOG.md`. | `3/5` | The same closeout pass synchronized manager-facing unlock language and the remaining `DaisyHostUnitTestsRunner.exe` blocker without pretending the latest sprint had already rerun the full gate. | Still heavier than necessary for a single-file note, but appropriate for this multi-doc state change. | Reuse when DaisyHost work changes both product status and verification truth at the same time. |
| 2026-04-23 | `subagent-driven-development` | Parallel WS6/WS7 execution across app-core, docs, and main-integrator slices | `4/5` | Helped split the package into bounded lanes: the docs slice preclaimed the work cleanly and the app-core slice landed most of the MetaController work while the main thread owned session/render/processor integration. | `3/5` | Worker 3 produced a clean docs-only preclaim for `PROJECT_TRACKER.md` and `SKILL_PLAYBOOK.md`; the app-core lane materially advanced `HostedAppCore`, `MultiDelayCore`, `CloudSeedCore`, and their tests before the main thread closed both agents and verified the combined result at `89/89`. | The app-core worker never returned a final summary before shutdown, so the main thread had to inspect and integrate its workspace changes manually. | Useful for bounded parallel DaisyHost slices, but it still needs a disciplined integrator pass before verification claims. |
| 2026-04-23 | `superpowers:test-driven-development` | Combined WS6 MetaControllers + WS7 rack-ready session/render contract | `5/5` | Kept the implementation contract-first: red coverage for missing session/render/snapshot fields existed before the closeout pass, then the package moved through targeted green tests and only after that through the full host gate. | `5/5` | The WS6/WS7 slice reran `cmake --build build --config Release --target unit_tests`, then `ctest --test-dir build -C Release --output-on-failure -R "(MultiDelayCoreTest|CloudSeedCoreTest|HostSessionStateTest|RenderRuntimeTest|EffectiveHostStateSnapshotTest)"` passed `44/44`, and the full wrapper-driven host gate later passed `89/89`. | Parallel slices introduced one contract drift: `MetaControllerDescriptor` initially missed the planned `stateful` field and needed an integrator correction before final verification. | Strong reuse for any DaisyHost feature that changes shared contracts across app cores, state, and render/runtime together. |
| 2026-04-23 | `verification-before-completion` | WS6/WS7 closeout with cross-slice integration and doc updates | `5/5` | Prevented treating the targeted `44/44` pass as sufficient and forced the wrapper-driven full host gate before updating the checkpoint, tracker, README, and changelog. | `5/5` | `cmd /c build_host.cmd` reran the full host gate and `ctest` passed `89/89`, including the smoke tests, before the docs were updated to claim WS6 landed and WS7 groundwork source-backed. | The full proof loop is still slower than targeted verification and depends on the wrapper because the raw MSBuild path still inherits the shell-local `Path` / `PATH` split. | Reuse on every DaisyHost milestone closeout, especially when multiple slices have already landed code locally. |
| 2026-04-23 | `doc-coauthoring` | WS6/WS7 cross-doc closeout after the `89/89` rerun | `3/5` | Helped keep the new macro layer, rack-ready state/render wording, updated test counts, and manager-facing unlock language aligned across `README.md`, `PROJECT_TRACKER.md`, `CHECKPOINT.md`, `SKILL_PLAYBOOK.md`, and `CHANGELOG.md`. | `3/5` | The same closeout pass updated all five tracking docs after the verified rerun, replacing stale preclaim-only language with dated landed-status wording and concrete test evidence. | Still heavier than necessary for a small one-file maintenance edit. | Reuse for future DaisyHost closeouts that change both implementation truth and the repo's manager-facing status narrative. |
| 2026-04-22 | `superpowers:test-driven-development` | Workstream-6 CloudSeed hosted-app pilot (`DaisyCloudSeedCore`, app adapter, render smoke) | `5/5` | Forced the CloudSeed package to start with a real red step: the first `unit_tests` build failed for the missing wrapper/app headers, then the implementation was driven until the new core/app/render slice and the full host gate both went green. | `5/5` | Red build failed on missing `daisyhost/DaisyCloudSeedCore.h` and `daisyhost/apps/CloudSeedCore.h`; targeted CloudSeed slice passed `24/24`; full host gate passed `79/79`, including standalone/render smoke. | The red phase also surfaced an upstream MSVC portability defect in `RandomBuffer.cpp`, so the skill still needed repo-aware judgment about whether the fix belonged in host code or the imported source. | Strong reuse for DaisyHost app pilots where the cleanest contract can be specified in helper-layer and render tests before the UI/processor glue is trusted. |
| 2026-04-22 | `verification-before-completion` | Workstream-6 CloudSeed closeout with processor-binding refresh | `5/5` | Prevented stopping at "the new tests pass" and forced the exact JUCE target set plus full `ctest` rerun after the processor binding refresh and smoke-harness changes. | `5/5` | After targeted CloudSeed tests passed, the full sanitized build command rebuilt `unit_tests`, `DaisyHostHub`, `DaisyHostRender`, `DaisyHostPatch_VST3`, and `DaisyHostPatch_Standalone`, then `ctest --test-dir build -C Release --output-on-failure` passed `79/79`. | The full proof loop still needs the shell-local `Path` / `PATH` sanitization step before MSBuild-backed commands. | Reuse on every DaisyHost app addition or processor/UI integration change, not just on infrastructure work. |
| 2026-04-22 | `systematic-debugging` | Verification-hardening pass after the new host-build wrapper exposed a VST3 post-build crash | `4/5` | Prevented a wrapper-level band-aid: the failure was reproduced outside the wrapper, narrowed to `juce_vst3_helper.exe` succeeding in PowerShell but failing through the MSBuild/`cmd` launch path, and then fixed with a PowerShell-backed manifest helper script. | `5/5` | `powershell -ExecutionPolicy Bypass -File .\build_host.ps1` exposed the failing VST3 target; direct helper invocation succeeded from PowerShell; `cmd`-style invocation failed with `LoadLibraryW failed for path ""`; the CMake fix plus `tools/write_vst3_manifest.ps1` restored `.\build_host.cmd` to green. | Needed extra repro steps outside the main build because the first failure looked like a generic VST3 target break. | Strong reuse whenever DaisyHost hits opaque JUCE/MSBuild post-build failures. |
| 2026-04-22 | `verification-before-completion` | Wrapper/factory verification hardening plus firmware proof closeout | `5/5` | Forced the session to keep going after the first wrapper run exposed a real VST3 build failure, and required fresh wrapper-driven host-gate evidence plus fresh firmware `make` evidence before closeout. | `5/5` | `.\build_host.cmd` passed `71/71`; `make` passed in `patch/MultiDelay/`; `patch/Torus` was forced from an up-to-date response into a fresh rebuild after PowerShell removal of `build/`. | The proof loop is slower when a verification command uncovers an unrelated build defect that then has to be debugged first. | Reuse on every DaisyHost verification, remediation, or milestone-closeout pass. |
| 2026-04-22 | `superpowers:test-driven-development` | W4/W5 helper-layer implementation for the DAW automation bridge and effective-state snapshot contract | `5/5` | Forced a clean red step first: the first `unit_tests` build failed on unresolved helper symbols, then one real contract assertion failed before green implementation and the final `71/71` host gate. | `5/5` | Red build failed for the expected missing helper entrypoints; targeted helper tests passed `6/6`; full sanitized host gate passed `71/71`. | C++/JUCE work still needed header/interface scaffolding before the first red build could link, and one `effectiveNormalizedValue` assumption had to be corrected. | Strong reuse for future DaisyHost feature work that can be isolated into helper-layer tests before processor glue. |
| 2026-04-22 | `verification-before-completion` | Final W4/W5 verification and closeout discipline | `5/5` | Prevented treating a timed-out partial target build as success and forced a fresh rerun of the exact full host-gate command before closeout. | `5/5` | After a partial build timed out, the full sanitized command was rerun successfully: `cmake -S . -B build`, the full Release target set, then `ctest --test-dir build -C Release --output-on-failure` with `71/71` passing. | The full pass is slower and requires the shell-local `Path` / `PATH` sanitization step. | Reuse on every DaisyHost milestone or user-facing completion claim. |
| 2026-04-22 | `doc-coauthoring` | Multi-doc verification-hardening closeout after wrapper/firmware remediation | `3/5` | Helped keep the wrapper entrypoint, VST3 manifest workaround, firmware rerun evidence, and post-WS7 DAW-validation deferral aligned across `README.md`, `PROJECT_TRACKER.md`, `CHECKPOINT.md`, and `CHANGELOG.md`. | `3/5` | The same closeout pass updated all four tracking docs plus the skill playbook without reintroducing stale manual-gap wording for the shell issue. | Still heavier than necessary for small one-file documentation edits. | Reuse for multi-doc closeouts where verification truth changes in more than one place. |
| 2026-04-22 | `doc-coauthoring` | Multi-doc DaisyHost maintenance and workflow synchronization | `3/5` | Helped split skill-specific content out of the tracker and keep `README.md`, `AGENTS.md`, `PROJECT_TRACKER.md`, `CHECKPOINT.md`, `CHANGELOG.md`, and `LATEST_PROJECTS.md` aligned around the new file. | `3/5` | Cross-doc terminology stayed consistent; the dedicated skill file did not become orphaned; tracker and local-doc entrypoints now point to the same skill-governance model. | Workflow is broader and heavier than needed for small maintenance passes. | Reuse for larger DaisyHost doc refactors; avoid for trivial single-file edits. |

## Update Rules

- If a skill was materially used in an iteration, update both:
  - `PROJECT_TRACKER.md` to record that it was used in the iteration
  - `SKILL_PLAYBOOK.md` to record the evidence and any `Observed UF` change
- Do not increase `Observed UF` without citing concrete DaisyHost evidence.
- Do not copy a skill description into `Observed UF`; validation must come from
  actual task outcomes.
- Revisit `Expected UF` if repeated DaisyHost evidence shows the prior estimate
  was consistently wrong.
